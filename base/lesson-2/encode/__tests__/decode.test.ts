// import {
//   AvailableType,
//   createUint8ArrayFilledBits,
//   decode,
//   encode,
// } from "../index";

describe("decode - функция принимает на вход ArrayBuffer и схему декодирования на выходе массив данных", () => {
  // test("На входе массив и схема", () => {
  //   const expected = [2, 3, true, false, "ab"];
  //   const schema: [number, AvailableType][] = [
  //     [3, "number"],
  //     [3, "number"],
  //     [1, "boolean"],
  //     [1, "boolean"],
  //     [16, "ascii"],
  //   ];

  //   const bufferView = new Uint8Array([78, 97, 98]);

  //   expect(decode(bufferView.buffer, schema)).toMatchObject(expected);
  // });

  test("", () => {});
});
